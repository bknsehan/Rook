# This program intentionally contains semantic errors (wrong call arity and an
# undefined identifier). `rokade` must reject it. The exact message is not
# asserted here, only that compilation fails.
